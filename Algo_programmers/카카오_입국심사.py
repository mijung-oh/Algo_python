def solution(n, times):
    answer = 0
    simsa = len(times)
    while 1:
        # 입국심사관보다 줄이 더 길경우는 자리가 나는대로 심사를 받는다.
        if n >= len(times):
            pass
        # 대기인원수가 
    return answer