N = int(input())

card = list(map(int, input().split(' ')))

dp = [0] * (N+1)


if n == 1:
    dp[1] = card[0]

else:
    for i in range(2, N+1):
        for j in range()
        dp[i] = max(card[i-1], )